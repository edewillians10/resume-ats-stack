- name: Install Dodcker
  apt:
    name: docker.io
    state: present

- name: Enable Docker
  systemd:
    name: docker
    enabled: true
    state: started

- name: Install docker-compose plugin
  apt:
    name: docker-compose-plugin
    state: present
